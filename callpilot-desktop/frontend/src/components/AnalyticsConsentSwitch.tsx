'use client';

// Stub for the deleted AnalyticsConsentSwitch component. CallPilot removed
// PostHog analytics during adaptation.

import React from 'react';

interface Props {
  checked?: boolean;
  onCheckedChange?: (value: boolean) => void;
}

const AnalyticsConsentSwitch: React.FC<Props> = () => null;

export default AnalyticsConsentSwitch;
export { AnalyticsConsentSwitch };
