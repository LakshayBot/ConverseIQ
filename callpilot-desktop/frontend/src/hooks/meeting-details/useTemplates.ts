'use client';

// Stub for the deleted useTemplates hook.

export function useTemplates() {
  return {
    templates: [],
    selectedTemplate: null,
    isLoading: false,
    error: null,
    refresh: async () => {},
  };
}
