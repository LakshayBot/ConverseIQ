// ============================================================================
// Grain — the film layer. One fixed element over everything.
// ============================================================================

export function Grain(): React.JSX.Element {
  return <div aria-hidden="true" className="grain" />
}
